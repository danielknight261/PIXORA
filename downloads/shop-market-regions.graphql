query {
  markets(first: 25) {
    nodes {
      id
      name
      handle
      primary
      status
      regions(first: 50) {
        nodes {
          id
          name
          ... on MarketRegionCountry {
            code
          }
        }
      }
    }
  }
}
