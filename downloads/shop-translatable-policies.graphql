query Policies {
  translatableResources(first: 20, resourceType: SHOP_POLICY) {
    edges {
      node {
        resourceId
        translatableContent {
          key
          value
          digest
          locale
        }
      }
    }
  }
}
